services:
  cs16-server-lan:
    build: .
    image: cs16-server-lan
    container_name: cs16-server-lan
    init: true
    network_mode: host
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges
    restart: unless-stopped
